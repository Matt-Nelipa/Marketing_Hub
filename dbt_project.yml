name: "marketing_hub"
version: "1.0.0"
profile: "marketing_hub"

model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

models:
  +materialized: view
  marketing_hub:
    staging:
      +schema: staging
    marts:
      +schema: marts

seeds:
  +schema: raw
  +quote_columns: false